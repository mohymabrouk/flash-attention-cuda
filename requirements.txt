# The CUDA-enabled PyTorch build must match the host driver/toolkit. Kaggle already
# supplies it, so this file deliberately does not reinstall torch or NVIDIA wheels.
numpy>=1.24
pandas>=2.0
pytest>=8.0
